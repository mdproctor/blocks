type: supervisor
routing:
  type: first-match
  guard: "priority > 5"
termination:
  - type: max-iterations
    iterations: 20
aggregation:
  type: collect-all
agents:
  - type: worker
    name: analyst
    description: "Analyses input data"
    capabilities:
      - data-analysis
  - type: worker
    name: reviewer
